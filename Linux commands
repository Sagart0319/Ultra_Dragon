#Present working directory / To check current location
pwd 

#List of files
ls

#long list / It will show files & folders, execution permissions, memory
ll

#change directory
cd

#one step back from folder
cd ..
--> 2 steps back cd ../..

#Root directory
cd /

#Home directory
cd ~

# / -> root related users will appear here
# ~ -> home directory (User's related data will be here)

#what we have used commands will be appear here
history

#It will show the today date
date

#Go to desktop
cd /users/desktop

#Show the list of hidden files
ls -a

#To create the file/ Text editors 
vi / nano / vim
I -> for insert
:wq! -> to save the file
:%d -> To delete the content in the file

#To view the content of the file
cat

#copy the files
cp

#To view hidden files
ls -a

#To remove the file in hidden format
mv .zebra zebra

# create 0 byte file (Empty file)
touch file name

# Create multiple files
touch a b c d

# delete the file
rm file name

# create directory
mkdir test

# create multiple directories
mkdir a b c d

# delete the directory
rm -rf folder name

# delete all the directories
rm -rf*

# move the file from one place to another place 
# rename the file
mv file name /path of the file
mv actualfilename renamefilename

# Folder inside folder will create
mkdir -p a/b/c/d

# > - single redirection / overide the data when enter the data second time / first time written data will overide
echo "write the content of the file" > file name

# >> - double redirection / dont overide the data when enter the data second time
echo "write the content of the file" >> file name

#Check the word count of the file
wc
wc -l -> show the data lines count

#Print top 5 lines in the file
head -5 filename

#Print last 2 lines in the file
tail -2 file name

#Grep command is used for search paticular key word in the file
grep
grep d filename
d -> file data will be print

# import the data in the file / import the logs in the file
-> If you mamnager said send the filelist on the location - might be 800 files in this case
ll > file name
ps -ef > file name

# check the processing files
ps -ef
ps -ef | grep httpd -> To filterout the data grep command is useful

# check the list of files
ls

# check the long list files (we can see file permissions / time stamp)
ll

# switch to root user
sudo su -

#check the user details
cat /etc/passwd

#opreating system details
cat /etc/os-release

#cpu info
cat /proc/cpuinfo

#to check logs
tail -f /var/log/messages

#compress the folder
tar -cvf test.tar foldername

-cvf -> create verbose file
test.tar -> new zip name want to create

#To unzip
tar -xvf filename

#Cpu utilization
top

#show the memory
free -m

#check the folder structure
tree

#To print the opeating system name
uname -a\

#print the current user
id

#disk free/ file system/ available space
df
df -h

#disk free -> human reable format
du -sk

#check running port
netstat -na

#change the file permissions
rwx-rwx-rwx

rwx - owner
rwx - user
rwx - group / others

r - read = 4
w - write = 2
x - execute = 1

chmod 777 file name -> full access

#change the owner permissions
chown ec2-user:ec2-user filename

chown - change owner permission
ec2-user - owners / Mention the required user name to change the permissions

chmod -> 
# check the public ip of Ec2
curl ifconfig.me

# check the private ip of Ec2
hostname
hostname -I | awk '{print $1}'

#
